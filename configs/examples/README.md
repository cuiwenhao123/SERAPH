# Example Configs

Reserved for sample configuration files that demonstrate repository conventions without representing runtime output.

Current examples:

- `seraph.third_party.env.example`: split Phase 2 embedding and Phase 3 LLM environment template for OpenAI-compatible providers such as SiliconFlow or local Responses gateways.
