[package]
name = "s3-extract"
version.workspace = true
edition.workspace = true
license.workspace = true
publish = false
description = "Phase 1 raw information extraction for SERAPH."

[dependencies]
serde_json.workspace = true
seraph-types = { path = "../seraph-types" }

[dev-dependencies]
serde_json.workspace = true
seraph-types = { path = "../seraph-types" }
