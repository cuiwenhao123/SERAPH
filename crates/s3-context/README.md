# s3-context

This crate is reserved for preparing stage-specific minimal context and for mapping split operations such as `split-mapping`.

It bridges stable repository data into stage-friendly context artifacts without becoming the system control plane.
